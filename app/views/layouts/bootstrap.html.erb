<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>enRails</title>
    <meta name="description" content="">
    <meta name="author" content="">
   
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "jquery.prettyPhoto" %>
    <%= javascript_include_tag "bootstrap-twipsy" %>
    <%= javascript_include_tag "bootstrap-popover" %>
    <%= javascript_include_tag "bootstrap-buttons" %>
    <%= javascript_include_tag "bootstrap-alerts" %>
    <%= javascript_include_tag "bootstrap-tabs" %>
    <%= javascript_include_tag "bootstrap-dropdown" %>
     <%= javascript_include_tag "jquery.validate" %>
<script type="text/javascript" charset="utf-8">
$(document).ready(function(){
  $("a[rel='prettyPhoto[pp_gal]']").prettyPhoto();
   $("a[rel='prettyPhoto']").prettyPhoto();
   $(".alert-message").alert();
   $('#blog').popover('show');
	$("#form").validate({
        rules: {
            "email": {
                required: true,
                email: true
            },
            "text": {
                required: true
            }
        },
        errorElement: "span",
        //wrapper: "div",  // a wrapper around the error message
        errorPlacement: function(error, element) {
            offset = element.offset();
            error.insertBefore(element)
            error.addClass('help-inline');  // add a class to the wrapper
            error.css('position', 'absolute');
            error.css('left', offset.left + element.outerWidth());
            error.css('top', offset.top);
        }

    });
});
</script>
   
     <%= stylesheet_link_tag "prettyPhoto" %>
     <%= stylesheet_link_tag    "application" %>
    <%= stylesheet_link_tag    "bootstrap" %>
    
    
   <style type="text/css">
      /* Override some defaults */
      html, body {
        background-color: #eee;
      }
      body {
        padding-top: 40px; /* 40px to make the container go all the way to the bottom of the topbar */
      }
      .container > footer p {
        text-align: center; /* center align it with the container */
      }
      .container {
        width: 820px; /* downsize our container to make the content feel a bit tighter and more cohesive. NOTE: this removes two full columns from the grid, meaning you only go to 14 columns and not 16. */
      }

      /* The white background content wrapper */
      .container > .content {
        background-color: #fff;
        padding: 20px;
        margin: 0 -20px; /* negative indent the amount of the padding to maintain the grid system */
        -webkit-border-radius: 0 0 6px 6px;
           -moz-border-radius: 0 0 6px 6px;
                border-radius: 0 0 6px 6px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
                box-shadow: 0 1px 2px rgba(0,0,0,.15);
      }

      /* Page header tweaks */
      .page-header {
        background-color: #f5f5f5;
        padding: 20px 20px 10px;
        margin: -20px -20px 20px;
      }

      /* Styles you shouldn't keep as they are for displaying this base example only */
      .content .span10,
      .content .span4 {
        min-height: 500px;
      }
      /* Give a quick and non-cross-browser friendly divider */
      .content .span4 {
        margin-left: 0;
        padding-left: 19px;
        border-left: 1px solid #eee;
      }

      .topbar .btn {
        border: 0;
      }

    </style>

    <!-- Le fav and touch icons 
    <%= link_to image_tag('favicon.ico'),:rel=>"shortcut icon"%>
    <link rel="shortcut icon" href="assets/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">
 -->
 
 
 	
  </head>

  <body>
 
    <div id="fb-root"></div>
<script>(function(d, s, id) {
  var js, fjs = d.getElementsByTagName(s)[0];
  if (d.getElementById(id)) return;
  js = d.createElement(s); js.id = id;
  js.src = "//connect.facebook.net/es_LA/all.js#xfbml=1&appId=100461830066517";
  fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>
 
    <div class="topbar">
      <div class="fill">
        <div class="container">
          <%=link_to 'enRails',root_path,:class=>'brand'%>
          <ul class="nav">
           <%=render 'enrails/sections'%>
          </ul><!--
          <form action="" class="pull-right">
            <input class="input-small" type="text" placeholder="Username">
            <input class="input-small" type="password" placeholder="Password">
            <button class="btn" type="submit">Sign in</button>
          </form>-->
        </div>
      </div>
    </div>

    <div class="container alpha60">

      <div class="content">
        <div class="page-header">
          <h1>enRails <small>We are riding</small></h1>
        </div>
        <div class="row">
          <div class="span10">
           <% if @section && params[:controller]=='enrails'%>
           <h1><%= @section.tituloes%></h1>
           <%end%>
            <%= yield%>
          </div>
          <div class="span4">
            <!--<h3>Idioma: <%=link_to alternaidioma,root_path(:local=>alternaidioma)%></h3>-->
    		
            <%=link_to image_tag('enrailsblack.jpg',:size=>'128x128',:alt=>'enRails'), '/assets/enrailsblack.jpg',:rel=>'prettyPhoto',:title=>'enRails'%>
            
             <%if logado%> 
              <h3>Gestión</h3>
              <%=render 'enrails/options'%>
          	  <%=link_to 'Desconectar',logout_path%>
          	  <%else%>
          	  <h3>Siguenos en</h3>
          	<a href="https://twitter.com/enrails" class="twitter-follow-button" data-show-count="false" data-lang="es" data-size="large">Follow @enrails</a>
<script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0];if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src="//platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs);}}(document,"script","twitter-wjs");</script></a>
<br/>
<br/>
<div class="fb-like-box" data-href="http://www.facebook.com/enrails" data-width="292" data-show-faces="false" data-stream="false" data-header="false"></div>

          	 <%end%>
          </div>
        </div>
      </div>

      <footer>
        <p>&copy;enRails 2011</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
